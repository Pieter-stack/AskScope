



{% block body %}

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Surname</th>
                <th>Email</th>
                <th>Password</th>
                <th>ProfileUrl</th>
                <th>Rep</th>
                <th>Access</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_profile in user_profiles %}
            <tr>
                <td>{{ user_profile.id }}</td>
                <td>{{ user_profile.name }}</td>
                <td>{{ user_profile.surname }}</td>
                <td>{{ user_profile.email }}</td>
                <td>{{ user_profile.password }}</td>
                 <td>{{ user_profile.profileUrl }}</td>
                 <img src="{{ asset( '/profileUrl/' ~ user_profile.profileUrl ) }}" width="60px" height="60px"/>
                <td>{{ user_profile.rep }}</td>
                <td>{{ user_profile.access }}</td>
                <td>
                    <a href="{{ path('show', {'id': user_profile.id}) }}">show</a>
                    <a href="{{ path('edit', {'id': user_profile.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('new') }}">Create new</a>
{% endblock %}
